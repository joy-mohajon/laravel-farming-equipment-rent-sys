function handleOptionChange(selectElement) {
    var selectedOption = selectElement.value;

    // Update the selectedOption variable (you may want to use AJAX to send it to the server if needed)
    console.log("Selected Optionnnnnn:", selectedOption);
}
